Flask==2.3.2         # Web framework for the app
gunicorn==20.1.0     # WSGI server for deployment
Jinja2==3.1.2        # Template engine used by Flask
Werkzeug==2.3.6      # Flask dependency for routing and requests
itsdangerous==2.1.2  # Secure session management
click==8.1.3         # Command line interface library (Flask dependency)
